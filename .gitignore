__pycache__/
*.pyc
.DS_Store

# Separate side project — lives here for convenience, not part of the Atlas
howto-app/

# Large source PDF kept locally, not needed by the site
UN Database Source Analysis.pdf

# Local editor/agent config
.claude/

# Transient output of scripts/check_source_editions.py — regenerated on every
# run (locally and in CI); the GitHub Issues it produces are the real record.
watchdog_findings.json
